# 09_RISK_REGISTER

The living, authoritative risk register is `../RISK_REGISTER.md` at project root (per `../CLAUDE.md` directory map). This folder is reserved for deeper per-risk analysis notes if any single risk (most likely R1 attribution, R2 baseline gaming, or R6 internal resistance) grows large enough to need its own document in cycle two.
