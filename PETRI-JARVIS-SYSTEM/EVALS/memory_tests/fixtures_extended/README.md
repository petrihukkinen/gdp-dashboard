This is a **larger, domain-representative but entirely fabricated**
corpus — not real Petri Jarvis knowledge. No real corpus was available to
test against in this session (checked: no separate `PETRI-KNOWLEDGE` or
`PETRI-JARVIS-SYSTEM` repository exists yet under the account this
session has access to). It exists to measure Phase 1's search behavior —
especially Finnish word-form (morphology) handling — against content that
looks like plausible real usage (investment/broker decisions, a startup
cap table, a shareholder-agreement note), more thoroughly than the
minimal `fixtures/` corpus does. See `../test_extended_synthetic_eval.py`.
